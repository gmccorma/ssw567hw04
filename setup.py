# Attempt to fix problem with travis-ci not recognizing my imports.

from setuptools import setup, find_packages

setup(name='github_info',
      author='Gabrielle McCormack',
      packages=find_packages(),
     )
